matrix:
    include:
        -   os: linux
            dist: trusty
            language: python
            python: 2.7
        -   os: osx
install:
    - pip install -r requirements.txt
    - python setup.py install --user
script:
    - test/test.sh
